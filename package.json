{
  "name": "todolist-mern-auth",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "engine": {
    "node": "14.x"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently -n 'backend,frontend' -c 'red,green' \"cd server && npm run dev\" \"cd client && npm run start\"",
    "heroku-postbuild": "npm run install-client && npm run build",
    "install-client": "npm install client",
    "client": "cd client && npm run start",
    "build": "cd client && npm run build"
  },
  "author": "Cratonic Dev",
  "license": "ISC"
}
